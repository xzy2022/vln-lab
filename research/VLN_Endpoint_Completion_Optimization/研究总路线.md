# VLN Endpoint Completion Optimization：研究总路线

本文档是本研究方向的唯一总路线入口，用来确定研究问题、阶段划分、协议冻结原则和文档更新纪律。

每个阶段的完整实验结果、表格、失败分析和结论应该单独写入阶段成果文档；本文档只保留“路线级事实”和“当前决策状态”。后续每次更新文档时只更新一处：如果改的是总路线，只改本文档；如果补的是某个阶段结果，只改对应阶段文档。

## 0. 文档纪律

### 0.1 单一事实源

```text
总路线、阶段定义、冻结原则：只写在 研究总路线.md
阶段实验结果、命令、表格、产物路径：只写在对应阶段成果 md
临时想法、对话草稿、未审核推断：不直接混入冻结文档
```

如果某个结论需要同时出现在多个文档中，只保留一个 canonical 版本，其他地方只链接或引用，不复制展开。

### 0.2 更新规则

每次文档更新必须满足：

1. 只更新一个 markdown 文件。
2. 明确本次更新属于“总路线调整”还是“阶段成果补充”。
3. 没有人工审核前，新增判断一律标记为 `待审核`。
4. `val_unseen` 结果只能作为冻结协议后的最终报告，不能反向修改阈值、tau 或模型选择。

### 0.3 审核状态

本文档使用三个状态：

```text
draft：可讨论草案
reviewed：人工已读并认可口径
frozen：协议或结论已冻结，除非新证据足够强，否则不改
```

当前总路线状态：

```text
draft，待人工审核
```

## 1. 核心研究问题

当前研究不直接做 full RL，也不重训 SAME 主导航器，而是研究：

```text
SAME 已经走过但没有正确 STOP 的 endpoint failure，
能否通过离线诊断、保守 reranking 和 learned endpoint preference，
在低 harm 的前提下恢复一部分 oracle gap？
```

当前主线暂定名：

```text
Evidence-Adaptive Completion Optimization for VLN
```

更具体地说：

```text
用 episode-level diagnostics、endpoint preference 和 category-aware routing，
在不重训主导航器的情况下减少 SAME 的 recoverable failure。
```

## 2. 研究边界

### 2.1 做什么

```text
分析 final STOP / endpoint failure
估计 SAME 已访问 endpoint 的可恢复上界
测试 rule-only endpoint reranking 是否可靠
构造严格离线 endpoint learning 闭环
学习 conservative gate + ranker
在冻结配置下一次性报告 val_unseen
```

### 2.2 不做什么

```text
不做在线 RL rollout
不在 val_unseen 上调参
不把 oracle distance 用作训练/推理特征
不把 smoke score 当 learned method
不让多个文档同时维护同一张结果表
```

## 3. 文档结构

计划使用一个总路线文档加多个阶段成果文档：

| 文档 | 归属内容 | 状态 |
| --- | --- | --- |
| `研究总路线.md` | 研究问题、阶段定义、冻结原则、下一步决策 | 当前文件，draft |
| `阶段1_oracle_gap报告.md` | final/oracle gap、overshoot、stop-too-early 统计 | 待创建 |
| `阶段2_endpoint上界.md` | nearest endpoint oracle-style 上界 | 待创建 |
| `阶段3_heuristic_reranker.md` | rule-only rerank 结果与失败分析 | 待创建 |
| `阶段4_离线endpoint_learning闭环.md` | candidate group、gate、ranker、loss ablation | 待创建 |
| `阶段5_NavNuances_router.md` | category-aware routing 与扩展实验 | 待创建 |

阶段成果文档只在对应阶段完成或需要人工审核时更新。总路线文档不复制阶段表格，只记录阶段是否成立、是否冻结、下一步是否继续。

## 4. 总体路线

当前按 5 个阶段推进：

| 阶段 | 核心问题 | 产物 | 当前状态 |
| --- | --- | --- | --- |
| 1. oracle gap 报告 | SAME 是否存在 final STOP / endpoint 可恢复失败？ | oracle gap report | 已生成文档 `阶段1_oracle_gap报告.md` |
| 2. 无训练 endpoint 上界 | 如果能在访问过的 endpoint 中选对，理论空间有多大？ | nearest endpoint upper bound | 已生成文档 `阶段2_upper bound报告.md` |
| 3. heuristic reranker | 不训练模型、只靠 SAME trace 规则能否稳定 recover？ | rule-only rerank report | 已有原型结果，待拆入阶段文档 |
| 4. 离线 endpoint learning 闭环 | learned gate + ranker 能否比 rule-only 更稳？ | dataset/evaluator/gate/ranker/ablation | 4.1 已有代码；4.2 下一步 |
| 5. NavNuances router | 能否按样本类别决定何时启用 endpoint correction？ | category-aware routing report | 暂不启动 |

阶段 1-3 的路线作用是确认第 4 阶段是否值得做：

```text
阶段 1：确认有没有可恢复 failure
阶段 2：确认可恢复空间有多大
阶段 3：确认规则信号是否足够，或者是否需要 learned gate
```

当前路线级判断：

```text
有可恢复空间；规则有信号但泛化不稳；第 4 阶段需要 learned gate + ranker。
状态：待人工审核
```

## 5. 当前实验数据来源

当前 R2R 原型实验目录：

```text
experiment_outputs/0014_same_val_r2r_eval_only_same_s0_v5
```

该实验用于：

```text
阶段 1：oracle gap
阶段 2：nearest endpoint upper bound
阶段 3：heuristic reranker
阶段 4.1：endpoint candidate group + evaluator
```

注意：

```text
这是 R2R trace 原型，不等价于最终完整实验。
正式版本需要补 train / val_seen / val_unseen 的更标准闭环。
```

## 6. 阶段 4 的统一口径

阶段 4 统一命名为：

```text
离线 endpoint learning 闭环
```

旧称 `轻量 endpoint scorer / preference loss` 不再作为阶段名称使用；它只描述 4.3 / 4.4 中的一类模型或 loss。

阶段 4 的目标：

```text
用固定协议构造 endpoint candidate group，
在 train/dev 上学习和冻结 gate、ranker、tau，
然后一次性报告 val_unseen，
验证 learned endpoint preference 是否比 rule-only heuristic 更稳。
```

### 6.1 子阶段

| 子阶段 | 目标 | 产物 | 是否训练 |
| --- | --- | --- | --- |
| 4.1 数据与评测闭环 | 固定 candidate / label / pair / tau-curve 协议 | dataset builder + evaluator | 否 |
| 4.2 gate-only baseline | 学“何时不要动 final” | gate baseline report | 只训 gate |
| 4.3 pairwise ranker 主实验 | 学候选 endpoint 排序 | ranker + inference report | 是 |
| 4.4 loss ablation | 比 CE / pairwise / DPO / GRPO | ablation table | 是 |

### 6.2 协议冻结

4.1 后冻结：

```text
candidate = all trajectory steps
should_rerank = final_fail && oracle_success
pair types = success>fail, better_SPL>worse_SPL, final_success final>bad_nonfinal
target_scope = official
```

训练参数只在 train/dev 上定：

```text
model type
hidden dim
learning rate
loss weights
class weights
pair sampling ratio
```

推理参数只在 dev 上定：

```text
gate_threshold
tau
allow_change_final
top-k candidate restriction
```

`val_unseen` 只用于冻结配置后的最终报告。

### 6.3 当前 4.1 代码入口

```bash
python scripts/analysis/build_endpoint_candidate_groups.py \
  --experiment-dir experiment_outputs/<experiment_id> \
  --target-scopes official
```

```bash
python scripts/analysis/eval_endpoint_reranker.py \
  --experiment-dir experiment_outputs/<experiment_id> \
  --score-csv path/to/endpoint_scores.csv \
  --target-scope official \
  --split dev \
  --gate-thresholds 0.3,0.5,0.7 \
  --taus 0,0.05,0.1 \
  --allow-change-final true,false
```

4.1 evaluator 只定义统一评测出口，不训练 gate，不学习 ranker。后续 4.2 和 4.3 都应该输出同一格式的 score CSV，再交给 evaluator 生成 tau curve 和 recovery-harm curve。

## 7. 成功标准

第 4 阶段的最低成功标准：

```text
val_unseen delta_SR > 0
harm_rate <= 1pp
SPL 不下降
```

更强标准：

```text
recover >= 10% oracle gap
R2R val_unseen SR 提升 >= 0.85pp
harm_rate <= 1pp
```

如果第 4 阶段没有达到最低标准，不进入 NavNuances router 主实验，只保留为 negative result 或回到 failure category analysis。

## 8. 下一步

当前下一步只做一件事：

```text
创建并人工审核 阶段4_离线endpoint_learning闭环.md，
先写 4.1 数据与评测闭环的已实现内容，
再决定是否开始 4.2 gate-only baseline。
```

本文档本轮更新状态：

```text
2026-04-29：重建总路线草案，等待人工审核。
```
