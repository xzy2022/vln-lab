# 阶段 4.1：数据与评测闭环报告

本文档承接 `research/VLN_Endpoint_Completion_Optimization/研究总路线.md`，只记录阶段 4.1 的作用、运行入口、实现注意事项、当前产物和完成度判断。

当前状态：

```text
reviewed：人工已读并认可口径
```

实验数据来源：

```text
experiment_outputs/0017_same_val_train_eval_all_r2r_reverie_cvdn_soon_same_s0_trace4ds_train_eval_v1
```

需要给出：

1. 本阶段的具体作用，对于 `research/VLN_Endpoint_Completion_Optimization/研究总路线.md`
2. 代码运行入口，以及以实验数据来源为示例的具体命令、输出路径
3. 代码实现的注意事项
4. 当前实验结果与完成度判断

## 1. 本阶段作用

阶段 4.1 对应总路线中“离线 endpoint learning 闭环”的第一步：

```text
如何把 SAME trace 固定成可训练、可评测的 endpoint candidate group？
```

它的作用不是训练 gate，也不是训练 ranker，而是先把后续 learned method 必须共享的数据协议和评测出口固定下来：

```text
episode group：一个 episode 对应一个 endpoint candidate group
candidate：trajectory 中的每个 step 都是一个候选 endpoint
label：每个 candidate 是否成功、SPL、reward、nearest / best-success 标记
pair：由 candidate label 构造 preference pair
gate label：should_rerank = final_success == false AND oracle_success == true
evaluator：任意 gate / ranker 只要输出统一 score CSV，就能生成 tau curve 和 recovery-harm curve
```

对于总路线来说，阶段 4.1 的作用是把阶段 4 后续工作从“写一个模型试试看”变成严格离线闭环：

```text
4.2 gate-only baseline：只需要输出 gate_score / should_rerank_score
4.3 endpoint ranker baseline：只需要输出 candidate_score
4.4 frozen config：只在 train/dev 上选择 gate_threshold、tau、allow_change_final
4.6 val_unseen final report：只用冻结配置一次性报告，不反向调参
```

本阶段只回答“数据和 evaluator 是否已经统一”。`stop_prob_scores.csv` 来自当前真实 SAME trace 中的 candidate `stop_prob`，不是假数据；但它没有经过 4.2 / 4.3 的 gate 或 ranker 训练，所以这里只把它当作 evaluator 连通性测试分数，不作为 learned method 或阶段 4 的方法结论。

## 2. 代码运行入口

### 2.1 构造 endpoint candidate group

通用入口：

```bash
python scripts/analysis/build_endpoint_candidate_groups.py \
  --experiment-dir experiment_outputs/<experiment_id> \
  --target-scopes official
```

以当前实验数据来源为例：

```bash
python scripts/analysis/build_endpoint_candidate_groups.py \
  --experiment-dir experiment_outputs/0017_same_val_train_eval_all_r2r_reverie_cvdn_soon_same_s0_trace4ds_train_eval_v1 \
  --target-scopes official
```

默认输出目录：

```text
experiment_outputs/0017_same_val_train_eval_all_r2r_reverie_cvdn_soon_same_s0_trace4ds_train_eval_v1/endpoint_learning/
```

输出文件：

```text
manifest.json
candidate_groups/episode_groups.csv
candidate_groups/endpoint_candidates.csv
preference_pairs/preference_pairs.csv
eval_protocol/protocol.json
eval_protocol/eval_protocol_summary.csv
```

### 2.2 统一 evaluator

通用入口：

```bash
python scripts/analysis/eval_endpoint_reranker.py \
  --experiment-dir experiment_outputs/<experiment_id> \
  --score-csv path/to/endpoint_scores.csv \
  --target-scope official \
  --split dev \
  --gate-thresholds 0.3,0.5,0.7 \
  --taus 0,0.05,0.1 \
  --allow-change-final true,false
```

以当前 smoke score 为例：

```bash
python scripts/analysis/eval_endpoint_reranker.py \
  --experiment-dir experiment_outputs/0017_same_val_train_eval_all_r2r_reverie_cvdn_soon_same_s0_trace4ds_train_eval_v1 \
  --score-csv experiment_outputs/0017_same_val_train_eval_all_r2r_reverie_cvdn_soon_same_s0_trace4ds_train_eval_v1/endpoint_learning/eval_protocol/stop_prob_scores.csv \
  --target-scope official \
  --split dev \
  --gate-thresholds 0.5 \
  --taus 0,0.02,0.05,0.1,0.2 \
  --allow-change-final true
```

默认输出目录：

```text
experiment_outputs/0017_same_val_train_eval_all_r2r_reverie_cvdn_soon_same_s0_trace4ds_train_eval_v1/endpoint_learning/eval_protocol/
```

输出文件：

```text
endpoint_reranker_eval_manifest.json
endpoint_learning_items.csv
endpoint_learning_summary.csv
tau_curve.csv
recovery_harm_curve.csv
```

如果当前实验目录下缺少 smoke test 用的 `stop_prob_scores.csv`，先从 `endpoint_candidates.csv` 导出一份 evaluator 可读的 score CSV：

```bash
awk -F, 'BEGIN{OFS=","}
NR==1{
  for(i=1;i<=NF;i++){
    if($i=="candidate_id") cid=i;
    if($i=="stop_prob") sp=i
  }
  print "candidate_id","candidate_score";
  next
}
{
  score=$sp;
  if(score=="") score="0.0";
  print $cid,score
}' \
  experiment_outputs/0017_same_val_train_eval_all_r2r_reverie_cvdn_soon_same_s0_trace4ds_train_eval_v1/endpoint_learning/candidate_groups/endpoint_candidates.csv \
  > experiment_outputs/0017_same_val_train_eval_all_r2r_reverie_cvdn_soon_same_s0_trace4ds_train_eval_v1/endpoint_learning/eval_protocol/stop_prob_scores.csv
```

这里的 `0.0` 只用于没有可对齐 decision trace、因此没有 `stop_prob` 的 route-expanded candidate。后续 4.2 / 4.3 如果已经生成 `gate_scores.csv` 或 `ranker_scores.csv`，应直接把对应文件传给 `--score-csv`，不再使用这份 smoke score。

## 3. 代码实现注意事项

### 3.1 数据对齐

脚本复用阶段 1 / 2 的数据发现与 fine metrics 对齐方式：

```text
eval_items/*_eval_context.json
eval_items/*_eval_items.jsonl
fine_metrics/tables/fine_metrics_wide.csv
```

每条 item 通过下面的键和 `fine_metrics_wide.csv` 对齐：

```text
dataset
split
internal_item_id
```

如果 fine metrics 中缺少对应行，脚本会直接报错，而不是静默跳过。

### 3.2 candidate 协议

当前固定协议是：

```text
one episode = one group
one trajectory step = one candidate
不做 viewpoint deduplication
candidate = all trajectory steps
target_scope = official
```

这意味着 repeated viewpoint / loop / route-expanded intermediate step 都会保留为候选 endpoint。后续 ranker 需要自己学习“哪个 step 更值得停”，而不是由数据构造阶段提前删掉候选。

### 3.3 trace 对齐

SAME 的 `decision_trace.steps` 与 `prediction.trajectory` 不一定是一一对应关系。脚本通过 `current_viewpoint` 和 `route_viewpoints` 对齐：

```text
有 decision trace 的 step：保留 stop_prob、selected_prob、router entropy 等特征
route-expanded intermediate step：保留 label，但 has_decision_trace=false
无法对齐的 trace step：不强行写入 candidate
```

这点很重要：candidate label 以 trajectory step 为准，模型可用特征以可对齐的 SAME decision trace 为准。

### 3.4 train / dev / test 协议

当前协议：

```text
val_train_seen：stable hash split，dev_ratio = 0.2
val_unseen：test/report only
dev_salt：endpoint_learning.v1
```

也就是说：

```text
train：用于训练 gate / ranker
dev：用于选择 gate_threshold、tau、allow_change_final
test：对应 val_unseen，只能用于冻结配置后的最终报告
```

### 3.5 label 与 feature 边界

candidate 可用于训练 / 推理的 trace features 包括：

```text
is_final
has_decision_trace
step_frac
path_length_m
stop_prob
stop_margin_prob
selected_prob
top1_top2_margin
moe_router_entropy
fuse_weight
is_revisit
is_loop_region
is_last_k
```

下面字段只能用于离线 label、loss 或 evaluation，不能作为 gate / ranker 的推理输入：

```text
success_label
spl_at_candidate
distance_to_goal_m
reward
is_best_success_candidate
is_nearest_candidate
final_success
oracle_success
nearest_endpoint_success
should_rerank
```

其中 `distance_to_goal_m` 是 GT distance，必须严禁进入 inference feature。

### 3.6 pair 构造

当前构造三类 preference pair：

```text
success_gt_fail：成功 candidate > 失败 candidate
better_spl_success_gt_lower_spl_success：高 SPL 成功 candidate > 低 SPL 成功 candidate
final_success_final_gt_failed_nonfinal：final 已成功时，final > 失败 non-final
```

第三类 pair 是阶段 3 失败分析之后加入的保守约束：它服务于“不要轻易伤害原本成功的 final endpoint”。

### 3.7 evaluator 协议

evaluator 不关心 score 来自规则、gate、ranker 还是 ablation，只要求输入 score CSV 至少包含：

```text
candidate_id
candidate_score
```

可选包含：

```text
gate_score
```

当前推理规则：

```text
if gate_score < gate_threshold:
    choose final
else:
    choose argmax candidate_score
    if best_score <= final_score + tau:
        choose final
```

输出指标统一为：

```text
SR / SPL / delta_SR / delta_SPL
recovery_rate / harm_rate / net_recovery_rate
changed_endpoint_rate
gate_precision / gate_recall / gate_auc
tau_curve
recovery_harm_curve
```

## 4. 当前实验结果

以下结果使用：

```text
experiment = 0017_same_val_train_eval_all_r2r_reverie_cvdn_soon_same_s0_trace4ds_train_eval_v1
datasets = R2R, REVERIE, SOON, CVDN
target_scope = official
```

### 4.1 candidate group 汇总

`manifest.json` 中记录的总量：

```text
episode_groups = 72794
endpoint_candidates = 768705
preference_pairs = 1609639
eval_item_sources = 14
summary_rows = 16
```

按 dataset / protocol split 的汇总如下：

| dataset | split | protocol split | episodes | candidates | pairs | should rerank | final SR | oracle SR | decision-trace candidates | route-intermediate candidates | route-expanded candidates |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| R2R | train_eval | train | 14039 | 95962 | 218461 | 640 / 4.56% | 87.35 | 91.91 | 90417 / 94.22% | 5300 / 5.52% | 5545 / 5.78% |
| R2R | val_train_seen | dev | 1501 | 10141 | 23565 | 84 / 5.60% | 88.74 | 94.34 | 9648 / 95.14% | 472 / 4.65% | 493 / 4.86% |
| R2R | val_seen | seen_test | 1021 | 7160 | 14997 | 65 / 6.37% | 80.22 | 86.58 | 6745 / 94.20% | 393 / 5.49% | 415 / 5.80% |
| R2R | val_unseen | unseen_test | 2349 | 18926 | 44962 | 200 / 8.51% | 76.29 | 84.80 | 16647 / 87.96% | 2197 / 11.61% | 2279 / 12.04% |
| REVERIE | train_eval | train | 10466 | 73117 | 123851 | 857 / 8.19% | 81.52 | 89.71 | 69259 / 94.72% | 3614 / 4.94% | 3858 / 5.28% |
| REVERIE | val_train_seen | dev | 123 | 816 | 1419 | 12 / 9.76% | 83.74 | 93.50 | 795 / 97.43% | 20 / 2.45% | 21 / 2.57% |
| REVERIE | val_seen | seen_test | 1423 | 12194 | 13368 | 112 / 7.87% | 57.84 | 65.71 | 10378 / 85.11% | 1758 / 14.42% | 1816 / 14.89% |
| REVERIE | val_unseen | unseen_test | 3521 | 38199 | 40950 | 310 / 8.80% | 45.84 | 54.64 | 29068 / 76.10% | 8826 / 23.11% | 9131 / 23.90% |
| SOON | train_eval | train | 22193 | 264933 | 652840 | 2802 / 12.63% | 71.23 | 83.85 | 236717 / 89.35% | 27457 / 10.36% | 28216 / 10.65% |
| SOON | train_eval | dev | 5607 | 67365 | 164497 | 666 / 11.88% | 71.98 | 83.86 | 59959 / 89.01% | 7235 / 10.74% | 7406 / 10.99% |
| SOON | val_seen | seen_test | 1130 | 22061 | 36378 | 117 / 10.35% | 51.24 | 61.59 | 14900 / 67.54% | 7040 / 31.91% | 7161 / 32.46% |
| SOON | val_unseen | unseen_test | 3390 | 63225 | 92819 | 599 / 17.67% | 36.34 | 54.01 | 45887 / 72.58% | 16851 / 26.65% | 17338 / 27.42% |
| CVDN | train_eval | train | 3796 | 59109 | 121573 | 816 / 21.50% | 45.71 | 67.20 | 52765 / 89.27% | 6097 / 10.31% | 6344 / 10.73% |
| CVDN | train_eval | dev | 946 | 13981 | 25383 | 214 / 22.62% | 45.03 | 67.65 | 12701 / 90.84% | 1239 / 8.86% | 1280 / 9.16% |
| CVDN | val_seen | seen_test | 382 | 6444 | 9424 | 72 / 18.85% | 28.27 | 47.12 | 5483 / 85.09% | 929 / 14.42% | 961 / 14.91% |
| CVDN | val_unseen | unseen_test | 907 | 15072 | 25152 | 263 / 29.00% | 24.04 | 53.03 | 12739 / 84.52% | 2271 / 15.07% | 2333 / 15.48% |

这个表说明 4.1 的数据闭环已经覆盖了：

```text
train：50534 episodes
dev：8177 episodes
seen_test：3956 episodes
unseen_test / val_unseen：10167 episodes
```

并且 `should_rerank` 数量与阶段 1 的 oracle gap 口径一致：

```text
R2R val_unseen should_rerank = 200
REVERIE val_unseen should_rerank = 310
SOON val_unseen should_rerank = 599
CVDN val_unseen should_rerank = 263
```

这说明阶段 4.1 的 gate label 与阶段 1 / 2 的 final failure / oracle success 口径是对齐的。

### 4.2 preference pair 汇总

| dataset | split | protocol split | success > fail | better SPL success > lower SPL success | final success final > failed non-final | total pairs |
| --- | --- | --- | ---: | ---: | ---: | ---: |
| R2R | train_eval | train | 148062 | 20177 | 50222 | 218461 |
| R2R | val_train_seen | dev | 16003 | 2304 | 5258 | 23565 |
| R2R | val_seen | seen_test | 9853 | 1533 | 3611 | 14997 |
| R2R | val_unseen | unseen_test | 31345 | 4843 | 8774 | 44962 |
| REVERIE | train_eval | train | 75007 | 4499 | 44345 | 123851 |
| REVERIE | val_train_seen | dev | 837 | 55 | 527 | 1419 |
| REVERIE | val_seen | seen_test | 8171 | 321 | 4876 | 13368 |
| REVERIE | val_unseen | unseen_test | 27076 | 1618 | 12256 | 40950 |
| SOON | train_eval | train | 470329 | 46776 | 135735 | 652840 |
| SOON | train_eval | dev | 118376 | 11168 | 34953 | 164497 |
| SOON | val_seen | seen_test | 26247 | 2657 | 7474 | 36378 |
| SOON | val_unseen | unseen_test | 70089 | 6370 | 16360 | 92819 |
| CVDN | train_eval | train | 96876 | 9866 | 14831 | 121573 |
| CVDN | train_eval | dev | 19997 | 1885 | 3501 | 25383 |
| CVDN | val_seen | seen_test | 7701 | 823 | 900 | 9424 |
| CVDN | val_unseen | unseen_test | 20920 | 2009 | 2223 | 25152 |

这说明后续 4.3 / 4.5 可以同时支持：

```text
二分类 / CE：直接用 success_label 或 reward
pairwise ranker：使用 success > fail 和 better SPL pair
保守 final-stay：使用 final success final > failed non-final pair
```

### 4.3 evaluator 连通性测试结果

当前 evaluator 使用 `stop_prob_scores.csv` 做了一次连通性测试：

```text
score = candidate stop_prob
gate_score = 缺省 1.0
gate_threshold = 0.5
split = dev
allow_change_final = true
tau = 0, 0.02, 0.05, 0.1, 0.2
```

这里的 `stop_prob_scores.csv` 与真实 candidate group 一一对应：

```text
endpoint_candidates.csv: 768705 candidates
stop_prob_scores.csv: 768705 scores
dev evaluator candidates: 92303 candidates
```

因此它使用的是真实实验 trace 里的候选点和 SAME stop probability。称为“连通性测试”只是因为它不是 learned gate / ranker 的输出，不能代表阶段 4.2 / 4.3 的方法能力。

结果如下：

| dataset | tau | items | final SR | selected SR | delta SR | final SPL | selected SPL | delta SPL | recovery | harm | changed endpoint |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| R2R | 0 | 1501 | 88.74 | 88.74 | 0.00 | 85.01 | 85.26 | 0.24 | 0.00 | 0.00 | 1.40 |
| R2R | 0.02 | 1501 | 88.74 | 88.74 | 0.00 | 85.01 | 85.26 | 0.24 | 0.00 | 0.00 | 1.40 |
| R2R | 0.05 | 1501 | 88.74 | 88.74 | 0.00 | 85.01 | 85.26 | 0.24 | 0.00 | 0.00 | 1.33 |
| R2R | 0.1 | 1501 | 88.74 | 88.74 | 0.00 | 85.01 | 85.26 | 0.24 | 0.00 | 0.00 | 1.27 |
| R2R | 0.2 | 1501 | 88.74 | 88.74 | 0.00 | 85.01 | 85.19 | 0.18 | 0.00 | 0.00 | 0.87 |
| REVERIE | 0 | 123 | 83.74 | 83.74 | 0.00 | 79.20 | 79.45 | 0.25 | 0.00 | 0.00 | 0.81 |
| REVERIE | 0.02 | 123 | 83.74 | 83.74 | 0.00 | 79.20 | 79.45 | 0.25 | 0.00 | 0.00 | 0.81 |
| REVERIE | 0.05 | 123 | 83.74 | 83.74 | 0.00 | 79.20 | 79.45 | 0.25 | 0.00 | 0.00 | 0.81 |
| REVERIE | 0.1 | 123 | 83.74 | 83.74 | 0.00 | 79.20 | 79.45 | 0.25 | 0.00 | 0.00 | 0.81 |
| REVERIE | 0.2 | 123 | 83.74 | 83.74 | 0.00 | 79.20 | 79.45 | 0.25 | 0.00 | 0.00 | 0.81 |
| SOON | 0 | 5607 | 71.98 | 71.98 | 0.00 | 65.33 | 65.78 | 0.45 | 0.00 | 0.00 | 3.05 |
| SOON | 0.02 | 5607 | 71.98 | 71.98 | 0.00 | 65.33 | 65.78 | 0.45 | 0.00 | 0.00 | 2.87 |
| SOON | 0.05 | 5607 | 71.98 | 71.98 | 0.00 | 65.33 | 65.77 | 0.44 | 0.00 | 0.00 | 2.75 |
| SOON | 0.1 | 5607 | 71.98 | 71.98 | 0.00 | 65.33 | 65.73 | 0.40 | 0.00 | 0.00 | 2.43 |
| SOON | 0.2 | 5607 | 71.98 | 71.98 | 0.00 | 65.33 | 65.59 | 0.26 | 0.00 | 0.00 | 1.52 |
| CVDN | 0 | 946 | 45.03 | 45.03 | 0.00 | 39.54 | 40.40 | 0.85 | 0.00 | 0.00 | 4.33 |
| CVDN | 0.02 | 946 | 45.03 | 45.03 | 0.00 | 39.54 | 40.40 | 0.85 | 0.00 | 0.00 | 4.23 |
| CVDN | 0.05 | 946 | 45.03 | 45.03 | 0.00 | 39.54 | 40.40 | 0.85 | 0.00 | 0.00 | 3.81 |
| CVDN | 0.1 | 946 | 45.03 | 45.03 | 0.00 | 39.54 | 40.40 | 0.85 | 0.00 | 0.00 | 3.38 |
| CVDN | 0.2 | 946 | 45.03 | 45.03 | 0.00 | 39.54 | 40.35 | 0.81 | 0.00 | 0.00 | 2.22 |

这个结果不能解释为“stop_prob 方法有效”。它只说明：

```text
score CSV 可以被 evaluator 正确读取
tau grid 可以正常展开
items / summary / tau_curve / recovery_harm_curve 可以正常输出
final-stay tau 逻辑可运行
```

从 0017 的 dev 结果看，`stop_prob` smoke score 在四个数据集上都没有带来 SR recovery，也没有 harm；它只在少量 endpoint 改动下带来轻微 SPL 变化。因此它仍然只能作为 evaluator 连通性测试，不能作为可用 reranker 结论。

由于没有真实 gate score，当前 `gate_score` 全部使用缺省值 1.0，因此：

```text
gate_pass_rate = 100%
gate_recall = 100%
gate_auc = 0.5
gate_precision = should_rerank rate
```

这些 gate 指标只是连通性测试中的占位 baseline，不代表 4.2 的 gate 能力。

### 4.4 与阶段 1 / 2 / 3 的衔接

阶段 1 给出的核心事实是：

```text
val_unseen oracle gap:
R2R = 200
REVERIE = 310
SOON = 599
CVDN = 263
```

阶段 4.1 中：

```text
R2R train/dev/unseen_test should_rerank = 640 / 84 / 200
REVERIE train/dev/unseen_test should_rerank = 857 / 12 / 310
SOON train/dev/unseen_test should_rerank = 2802 / 666 / 599
CVDN train/dev/unseen_test should_rerank = 816 / 214 / 263
```

因此 4.1 的 `should_rerank` 标签已经正确继承了阶段 1 的 oracle gap 口径。

阶段 2 证明 nearest endpoint 是 upper bound；阶段 4.1 保留：

```text
nearest_endpoint_success
nearest_endpoint_spl
is_nearest_candidate
```

这些字段只能用于 evaluation / label analysis，不能进入 inference feature。

阶段 3 证明 rule-only reranker 的主要风险是 harm 原本成功的 final endpoint；阶段 4.1 因此加入：

```text
final_success_final_gt_failed_nonfinal pair
allow_change_final
tau
harm_rate
final_success_harm_rate
```

这些字段和指标为 4.2 / 4.3 的 conservative gate + ranker 提供了统一评测出口。

## 5. 完成度判断

阶段 4.1 当前完成情况：

```text
完成度：基本完成，可以进入人工审核。
```

已经完成：

```text
1. candidate group builder 已实现
2. train / dev / test 协议已固定
3. candidate feature、label、pair 三类 CSV 已产出
4. GT label 与 inference feature 的边界已写入 protocol.json
5. evaluator 已实现统一 score CSV 入口
6. tau curve 与 recovery-harm curve 已能输出
7. 真实 trace 派生的 stop_prob score 已验证 evaluator 可以跑通
```

还没有完成、也不属于 4.1 的部分：

```text
1. 还没有训练 gate-only baseline
2. 还没有训练 endpoint ranker
3. 还没有选择 frozen gate_threshold / tau
4. 还没有报告 learned method 的 val_unseen 结果
```

因此，阶段 4.1 的结论是：

```text
数据与评测闭环已经打通。
4.1 不要求训练真实 gate / ranker；训练结果属于 4.2 / 4.3。
当前可以开始人工审核 4.1 协议。
协议审核通过后，可以进入 4.2 gate-only baseline。
```

本文档本轮更新状态：

```text
2026-04-29：补全阶段 4.1 数据与评测闭环报告草案，等待人工审核。
2026-04-29：已审核
2026-04-29：R2R val 切换到 R2R / REVERIE / SOON / CVDN 数据集的全部split
```
