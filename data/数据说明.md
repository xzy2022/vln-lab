

## data/same

使用
```bash
# 只下载数据集
python scripts/setup/download_same_assets.py \
  --datasets R2R REVERIE CVDN SOON \
  --levels all \
  --runtime mattersim \
  --models eval \
  --source hf-mirror

# 将 ckpt 和features/,simulator/等也下载
python scripts/setup/download_same_assets.py \
  --datasets all \
  --levels all \
  --runtime full \
  --models all \
  --source hf-mirror

```

- `R2R/*.json` 是 R2R 标注与评测 split。里面有指令、路径、scan id、viewpoint 等，属于“任务数据”。
- `features/img_features/clip_vit-b16_mp3d_hm3d_gibson.hdf5` 是预提取的视觉特征，SAME 推理时真正喂给模型的是它，不是原始 Matterport3D 场景图像。
- `simulator/connectivity/*.json`、`simulator/mp3d_connectivity_graphs.json`、`simulator/mp3d_scanvp_candidates.json` 是导航图结构、节点位置和候选动作信息。它们替代了真实 simulator 的在线图查询。
- `pretrain/Attnq_pretrained_ckpt.pt` 是“预训练初始化权重”。模型构建时会先从这里加载基础参数，`vlnbert_init.py` (line 18)。
- `ckpt/SAME.pt` 是“最终训练好的官方 checkpoint”。真正评测/恢复时，会再从这里覆盖加载完整任务权重，`base_trainer.py` (line 68) `base_trainer.py` (line 97)。
- 当前SAME子仓库自带的测试配置默认同时要求 pretrain 路径存在，`test.yaml` (line 1)。
